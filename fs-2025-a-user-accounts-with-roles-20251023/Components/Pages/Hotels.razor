@page "/hotels"
@using fs_user_accounts.Data
@using AppApp.Domain

<h3>Hotels</h3>

<p class="text-muted">This page uses the <code>HotelTable</code> component and the <code>MockData.Hotels()</code> factory.</p>

<!-- Omit Count to use the component default, or set Count explicitly, e.g. Count="30" -->
<HotelTable />