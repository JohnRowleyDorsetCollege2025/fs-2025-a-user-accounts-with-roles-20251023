@page "/courses"
@using fs_user_accounts.Data
@using AppApp.Domain

<h3>Courses</h3>

<p class="text-muted">This page uses the <code>CourseTable</code> component and the <code>MockData.Courses()</code> factory.</p>

<!-- Use the default Count or override with Count="30" -->
<CourseTable />