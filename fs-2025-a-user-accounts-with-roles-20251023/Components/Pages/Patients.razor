@page "/patients"
@using fs_user_accounts.Data
@using AppApp.Domain

<h3>Patients</h3>

<p class="text-muted">This page uses the <code>PatientTable</code> component and the <code>MockData.Patients()</code> factory.</p>

<PatientTable />